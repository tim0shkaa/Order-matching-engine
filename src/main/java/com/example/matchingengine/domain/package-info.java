/**
 * Доменные объекты matching engine: Order, Trade, OrderBook и связанные enum'ы.
 * <p>
 * Пакет не должен зависеть от Spring или любого другого фреймворка — только чистая Java.
 * Это осознанное ограничение: доменная модель должна быть тестируемой и переиспользуемой
 * без поднятия Spring-контекста.
 */
package com.example.matchingengine.domain;
